# 📓 高考数学一体化 Agent —— 开发历程记录（两天）

> 项目地址：https://github.com/wangzhaoxu392-boop/gaokao-math-agent
> 记录时间：2026-08-27 ~ 2026-08-28

---

## 一、项目是什么

一个**本地运行的数学教研与解题智能体**，基于 `LangGraph + Ollama 本地大模型`，实现四个功能一体化：

| 功能 | 作用 |
|---|---|
| ① 单题交互 | 输入一道数学题（可含多小问），自动拆题 → 逐步推理 → 输出完整标准答案 + 详细步骤，可自动画函数图像，支持追问 |
| ② RAG 知识库 | 把讲义/知识点放进 `knowledge_docs`，一键构建向量库，解题时自动检索参考 |
| ③ 批量做题 | 整份试卷放入 `batch_input`，自动逐题批改，输出 Word + PDF |
| ④ 真题教研 | 历年真题放入 `raw_exam_files`，自动提取题目并生成教研资料（标准答案/详细解答/解题方法/第二种解法/易错点/命题特点/一题三变），输出 Word + PDF |

**技术栈**：Python + LangGraph（状态图 Agent）+ Gradio（网页界面）+ Chroma（向量库）+ 本地 Ollama
**本地模型**：`deepseek-r1:14b`（主推理）、`qwen2.5:7b`（快速拆题）、`nomic-embed-text`（向量嵌入）
**全程本地运行，数据不出本机，无需云端 API。**

---

## 二、第一天（8/27）：从零搭建 + 首轮迭代

### 阶段 1 · 核心逻辑搭建（命令行版）

- 用 LangGraph 构建带状态的有向图 Agent，集成三个工具：
  - `sympy_math_calc`：符号计算（求导/解方程/数列求和等）
  - `plot_function`：绘制函数图像
  - `RAG`：基于 Chroma 的知识库检索
- 主模型负责深度推理，快速模型负责拆题等轻量环节，兼顾质量与速度
- 完成四大功能的核心逻辑，命令行菜单式交互（`math_agent_all.py`）

### 阶段 2 · 第一轮问题反馈与修复

| 用户反馈的问题 | 处理方式 |
|---|---|
| 题目答案过于啰嗦，没有完整标准答案 | 重构解答生成流程，输出**完整标准答案 + 分步过程 + 方法总结** |
| Word 文档出现很多乱码 | 定位到 MathType/Symbol 字体私有区乱码 → 建立符号映射表 `_SYMBOL_MAP`，把乱码还原成正常数学符号（π、α、β、⊥、→、△ 等） |
| 步骤要更详细 | 细化解题步骤输出层级 |
| 文件里多个习题能否正确输出 | 实现按"行首编号"切分多道习题（支持 `1.` / `1、` / `1．`），逐题批改并汇总输出 |
| 功能1 速度能否提升 | 拆分轻重环节，快速模型处理拆题 |

### 阶段 3 · 改为网页操作版

- 用户希望**使用界面变为网页版** → 用 Gradio 搭建 `math_agent_web.py`：
  - ① 单题交互：文本框输入 + 解答结果 + 函数图像
  - ② 知识库：文件上传（支持多选）+ 一键构建 + 日志
  - ③ 批量做题：文件上传 / 选择 + 处理日志 + 下载 Word/PDF
  - ④ 真题教研：同上
- 解决"重新开机怎么用""关掉 VS 是否也能用" → 编写 **`启动网页版.bat`** 一键启动脚本（自动检查并启动 Ollama → 拉起服务 → 打开浏览器），不依赖任何 IDE

### 阶段 4 · 部署到 GitHub 对外分享

- 用户提供 GitHub 账号（wangzhaoxu392-boop）→ 创建并推送公开仓库 `gaokao-math-agent`
- 添加 **MIT License**（明确他人可自由使用/修改）
- 配置仓库描述与标签
- 编写 `README.md`（功能说明 / 安装步骤 / 目录结构 / 工作原理）

---

## 三、第二天（8/28）：性能优化 + 功能修复 + 故障排查

### 阶段 1 · 网页版提速优化

| 用户反馈 | 处理方式 |
|---|---|
| 网页版速度非常慢 | ① 每次解题改用**独立会话**，避免历史消息无限累积拖慢 ② 知识库为空时**跳过向量检索**（不必每次加载嵌入模型）③ 增加**复核开关**，按需二次校验 |

### 阶段 2 · 功能1 记忆功能修复

- 网页版中"追问/记忆"失效 → 定位为**指代词解析**问题，修复后追问（如"那它的极值呢"）可正常衔接上下文

### 阶段 3 · 一键启动故障排查（重点）

- 现象：双击 `启动网页版.bat` 无反应 / 页面打不开
- 排查过程：
  1. 用 cmd 实际运行脚本 → **脚本本身是正常的**，服务能起、页面 HTTP 200
  2. 发现真正原因：**端口 7860 被残留的旧进程占用**，脚本误判"已在运行"只打开浏览器，但旧服务已死 → 页面打不开
- 加固脚本：
  - 先**确认页面真的返回 HTTP 200** 才走"已在运行"分支
  - 端口被占但页面不可用 → **自动清理僵尸进程**再重启
  - 等待服务真正就绪（最多 40 秒）再打开浏览器
  - 改为 UTF-8 编码，中文显示不乱码

### 阶段 4 · 双网页问题

- 现象：启动后同时打开两个网页
- 原因：网页代码里 `inbrowser=True`（Gradio 自动开一个浏览器）+ 启动脚本又开一个
- 修复：改为 `inbrowser=False`，由启动脚本统一只开一个浏览器

### 阶段 5 · 多文件处理支持

- 功能4（真题教研）无法一次多选几个文件 → 下拉框改为**可多选**，底层 `research_workflow` 增加 `only_files` 参数，只处理选中的文件（不选则处理全部，命令行版不受影响）
- 顺带检查功能3（批量做题）→ **发现同样问题**，一并改为多选，可一次处理多份试卷，输出多份 Word/PDF
- 检查功能2（知识库）→ 设计上就是"处理全部文件"，上传本就支持多选，**无此问题**；并确认其依赖的嵌入模型已安装

### 阶段 6 · Word 乱码彻底修复

- 之前发现的乱码问题做**根治**：
  - 新增 `clean_math_symbols()`：把 MathType 公式转出的私有区乱码统一还原为正常数学符号（应用于 PDF 读取全流程）
  - 新增 `_safe_json_loads()`：宽容解析 AI 输出的 JSON（自动清理非法控制字符、修复未转义换行），避免整段题目因一个解析错误被丢弃

### 阶段 7 · 同步到 GitHub

- 遭遇网络故障：`github.com` HTTPS 直连被重置（DNS 解析到被屏蔽的 IP）
- 排查路径：DNS 污染 → 换 IP 无效（TLS 被拦截）→ 发现**系统开着本地代理 `127.0.0.1:7897`** 但 git 默认不走系统代理
- 解决：为 git 配置 github.com 走本地代理 → 推送成功
- 今日更新全部同步：提交 `3da4b65`，本地与远程一致

### 阶段 8 · 功能3/4 实时进度显示

- 现象：网页版跑教研/批量做题，动辄数小时，日志框却一直空白，用户无法判断是否在推进
- 方案：
  - 底层 `research_workflow` / `batch_solve_file` 增加 `progress_cb` 进度回调
  - 网页端 `run_research` / `run_batch` 改为**「后台线程 + 生成器」**，把"正在分析第 X/Y 段""正在解答第 X/N 题"等进度**实时流式刷新**到日志框
- 效果：运行全程可见进度，不再是"黑箱等待"

### 阶段 9 · 教研运行故障排查 + 防卡死保护

- 现象：网页版对 3 份真题做教研，GPU 持续 97% 满载 **3 小时 20 分**，无任何结果输出
- 排查过程：
  1. 先确认网页服务正常（HTTP 0.004s）、进程存活、GPU 持续推理 → 一度判断"正常，只是慢"
  2. 深挖发现异常：**RTX 4070 上 `deepseek-r1:14b` 单次推理正常只需 1~2 分钟**，但 Ollama `/api/ps` 的 `expires_at` 连续 13+ 分钟冻结（同一请求始终未结束）→ 疑似 **deepseek-r1 无限思考**卡死
  3. 根因：模型调用**未设单次输出上限与超时**，超长推理出现时无兜底，只能一直挂着
  4. 最终该次运行在 18:23 自行完成（生成 28 题教研文档），但这类长时间单请求必须兜底
- 修复：给主/快速模型统一加 **`num_predict`（单次输出上限）+ `timeout`（超时兜底）**，即使再次出现超长推理也会自动终止并跳过，不会永久挂起
- 经验：**GPU 满载 ≠ 在推进**；单次推理超时数倍于正常值，即可判定异常

---

## 四、第三天（8/29）：功能2 升级为 Karpathy LLM Wiki

### 背景
- 了解 Andrej Karpathy 提出的 **LLM Wiki** 个人知识库模式（2026-04）：与其每次提问从原始文档临时检索（RAG），不如让 LLM 提前把资料“编译”成结构化的、互相链接的 Markdown Wiki，提问时直接读 Wiki（更快、知识可积累、带交叉引用）
- 用户希望把功能2（原 RAG 知识库）改造成这种模式

### 改造内容
- 新增 `wiki/` 层（编译产物）：
  - 按分类组织的**词条页**（概述 + 要点 + 关联词条 `[[交叉链接]]`）
  - `index.md` 总索引 + `_meta.json` 编译元信息
- 新增三个操作：
  1. **编译 Compile**：`build_wiki()` 读取 `knowledge_docs/`（原始层，只进不改）→ LLM（快速模型）逐块提炼词条 JSON → 写出词条页 + 索引 + 建向量检索库
  2. **查询 Query**：`wiki_retrieve()` 用 **n-gram 关键词打分（标题权重高）优先 + 向量检索补充**；解题时 `rag_retrieve` **Wiki 优先**，未编译自动回退旧 RAG
  3. **自检 Lint**：`wiki_lint()` 检查交叉链接是否断裂、索引是否失效、孤儿词条，必要时重建索引
- 网页版功能2 升级为「编译 Wiki 知识库」+「Wiki 自检」+ 备选「重建旧版 RAG」
- 命令行菜单功能2 同步改为编译 Wiki

### 验证结果
- 用 `knowledge_docs/知识点速查.txt` 实测：编译出 **11 个词条**（导数/数列/三角），自检无断裂链接
- 检索“用导数判断函数极值”正确命中「极值判定」词条；解题链路检索已切换为 Wiki 优先
- 语法检查通过

### 与旧 RAG 的关系
- 不删除旧 RAG：未编译 Wiki 时自动回退；网页端保留「重建旧版 RAG」备选按钮

### 补充：示例知识库与健壮性修复
- 实测放入 2 份资料（`知识点速查.txt` + `高中数学核心知识点讲义.md`），编译出 **42 个词条**（函数8/导数8/三角7/数列4/立体几何3/概率统计3/其他5），三类查询（圆锥曲线离心率、数列求和方法、导数求单调区间）全部正确命中对应词条
- 发现并修复两个健壮性问题：
  1. **大文件单块 JSON 截断**：`_split_knowledge_text` 增加单段硬切（≤800 字符/块），避免整份大文件挤成一块导致模型 JSON 输出过长被截断
  2. **解析失败丢数据**：快速模型 `num_predict` 1024→2048；`build_wiki` 增加**解析失败自动重试一次**，不再因单块失败丢整块
- 自检能正确报出"断裂链接 / 孤儿词条"，供用户掌握知识库质量
- **新增 `wiki_heal()` 自动修复**（Karpathy Lint/Heal 闭环）：扫描断裂链接，分类名自动降级为纯文本、与已有词条互为子串的自动重定向、无匹配的降级为纯文本；实测 12 个断裂链接一键修复为 0，网页端新增「Wiki 修复」按钮

### 数学解题质量审计（10 题实测）
- 测试覆盖：导数切线（过点/在点处）、数列裂项求和、圆锥曲线弦长、条件概率、基本不等式、函数单调区间、三角恒等变换、复数运算、排列组合
- **结果：9/10 正确**，仅"过点求切线"存在问题
- **问题1：过点求切线方法不稳定**——模型有时正确（先验证点是否在曲线上、用设切点法），有时错误（直接用点的横坐标求导）。属于**模型本身问题**（deepseek-r1:14b 概念一致性不足）
- **问题2：复杂方程计算错误**——当模型用设切点法列出三次方程 2a³-6a²-57=0 后，心算得出错误根 a=3（正确实根 a≈4.443）。属于**模型本身问题**（14B 模型复杂方程心算能力弱）
- **问题3：详细提示词导致空输出**——尝试加详细"过点求切线"规则强制模型用设切点法，结果 deepseek-r1:14b + 工具绑定返回空内容（提示词过长）。属于**模型/架构限制**
- **改进措施**：
  1. sympy_math_calc 工具增强：解方程自动返回数值解并标注实根/复根（不再返回恐怖的 Cardano 精确式）；预定义常用符号 x,y,z,a,b,c,n,k,t；支持 exec 多语句
  2. 解题提示词新增一行"高频易错提醒"（过点切线/不等式变号/圆锥曲线判别式），简短不导致空输出
  3. 结论：标准题型（数列/圆锥曲线/概率/不等式/三角/复数/排列组合）模型表现可靠；过点求切线等需复杂方程求解的题型，模型可能计算出错，建议人工复核关键数值

### 双模型自动路由（DeepSeek推理 + Qwen数学）
- 新增 SolveState 扩展 LangGraph 状态，增加 model_choice（auto/reasoning/math）和 model_used 字段
- 新增 llm_math 数学专用模型实例（默认 qwen2.5:14b，.env 的 MATH_MODEL 可配置），同步绑定工具
- 新增 route_model() 题型路由：含"证明/求证/是否存在/探究/讨论/说明理由"等推理关键词 → DeepSeek-R1；其余（默认）→ Qwen数学模型
- 新增 resolve_model() 根据用户选择+题目返回对应（带工具模型, 纯文本模型, 显示名）
- solve_agent / checker_agent 均改为按路由选择模型，返回 model_used 供输出标注
- run_question() 增加 model_choice 参数，返回 (答案, 使用模型) 元组
- 网页端功能1 新增模型选择下拉框：自动（按题型）/ DeepSeek-R1推理 / Qwen2.5-14B数学；输出开头标注"使用模型：XXX"
- 实测：qwen2.5:14b 对"过点求切线"方法完全正确（验证点不在曲线上→设切点法→列出正确方程 2x0³-6x0²-57=0），但三次方程心算仍出错（错误根 x0=3，正确≈4.443），属 14B 模型固有计算局限
- 尝试加强制 sympy 工具规则 → 导致模型方法退化（直接算 f'(2)）且不调用工具，故回退；14B 模型提示词规则过多会互相干扰
- 结论：双模型路由对标准题型有效提升（Qwen计算优于DeepSeek），复杂方程求解仍需人工复核或更大模型（32B+）

### 接入硅基流动 API（DeepSeek-V3 数学模型）
- 背景：本地 14B 模型对复杂方程（三次方程）心算不可靠（错误根 a=3，正确≈4.443），需更大模型
- 方案：数学模型支持双 provider——本地 Ollama（免费）或硅基流动 API（免费额度内，72B级模型更准）
- `.env` 新增：MATH_PROVIDER（ollama/siliconflow）、SILICONFLOW_API_KEY、MATH_MODEL
- 代码改造：llm_math 根据 MATH_PROVIDER 动态创建——siliconflow 用 ChatOpenAI（base_url=https://api.siliconflow.cn/v1），ollama 用 ChatOllama；resolve_model 显示名包含 provider 和模型名
- 安装依赖：langchain-openai
- 模型选型：硅基流动无 Qwen2.5-Math 专用模型，最终选用 deepseek-ai/DeepSeek-V3（数学计算极强、中文好、支持工具调用）
- 实测切线题 y=x³+58x 过点(2,59)：
  - 方法：✅ 完全正确（验证点不在曲线上→设切点法→正确方程 2a³-6a²-57=0）
  - 三次方程根：a≈4.4936（正确值≈4.4435，误差仅 1.1%）
  - 对比本地 14B：a=3（完全错误，误差 30%）
  - 结论：API 模型方法 100% 正确，数值精度大幅提升，可满足教学使用
- 路由策略：推理/证明题→本地 DeepSeek-R1:14b（免费不花钱）；计算/方程题→API DeepSeek-V3（免费额度内）
- 费用：每道题约 ¥0.01~0.02，新用户免费额度够解 1000+ 题

### 网页版 API 余额查询与用量统计
- 背景：用户希望在网页版直接查看硅基流动 API 剩余额度，无需每次登录网站
- 实现：新增 query_siliconflow_balance() 函数，调用 https://api.siliconflow.com/v1/user/info 获取余额（可用余额/充值余额/总余额/账户状态）
- 问题：硅基流动国内站（.cn）注册的 API Key 调用 .com 用户信息接口返回 401 未授权；.cn 的 /v1/user/info 已返回 410（已下线）
- 降级方案：API 查询失败时，显示费用中心链接（https://siliconflow.cn/account/fee）+ 本地用量统计
- 本地用量统计：api_usage.json 记录 API 解题次数和预估费用（每题 ¥0.015），run_question 中检测到 model_used 含 "siliconflow" 时自动记录
- 网页端：功能1 新增「查询API余额」按钮，点击显示余额/用量信息；下拉框标签改为「数学模型（API/本地）」
- 后续修复：按钮 lambda 改为带 try/except 的正式函数（避免异常时空白无响应）；网络超时 15s→8s；确认 .env 含 API Key 但已在 .gitignore 中且从未提交，GitHub 上传安全；更新 .env.example 补充 MATH_PROVIDER/SILICONFLOW_API_KEY/MATH_MODEL 配置项（仅占位符无真实密钥）
- 服务稳定性：PowerShell Start-Process 启动的进程会被系统回收导致网页打不开，改用后台任务或启动脚本（cmd /k）方式稳定运行

### 全功能模型选择下拉框 + 功能4接入API数学模型
- 背景：功能1已有模型选择下拉框，但功能2/3/4没有；功能4（教研汇总）一直用本地 DeepSeek 直接调用，未接入 API 数学模型
- 新增 get_llm_for_choice(choice, default) 辅助函数：根据 auto/reasoning/math 返回对应 LLM 实例（用于直接调用 .invoke() 的场景，非图模式），default 参数控制 auto 时默认用哪个模型（llm_fast/llm/llm_math）
- 功能2（Wiki编译）：build_wiki 新增 model_choice 参数，auto 默认用快速模型（llm_fast），可手动选 DeepSeek 或 API 数学模型编译
- 功能3（批量做题）：batch_solve_file 新增 model_choice 参数并传递给 run_question（原本就走自动路由，现在可手动强制）
- 功能4（教研汇总）：research_workflow 新增 model_choice 参数，题目提取和教研分析都改用选择的模型（research_llm），不再硬编码 llm；auto 默认用 DeepSeek 推理模型，可手动选 API 数学模型提升计算类教研分析精度
- 网页端：功能2/3/4 各新增模型选择下拉框（自动/DeepSeek-R1推理/数学模型API/本地），按钮点击事件传递 model_choice 到对应包装函数
- 四个功能的模型选择策略：
  - 功能1 单题：auto=按题型自动路由（证明→DeepSeek，计算→API数学）
  - 功能2 Wiki编译：auto=快速模型（qwen2.5:7b，编译快）
  - 功能3 批量做题：auto=按题型自动路由（同功能1）
  - 功能4 教研汇总：auto=DeepSeek推理模型（教研分析偏推理）

---

## 五、最终成果

- **代码规模**：`math_agent_all.py`（核心逻辑 ~40KB）+ `math_agent_web.py`（网页版 ~13KB）+ 启动脚本
- **Git 提交记录**（6 次）：
  1. `c29d663` 初始化：高考数学一体化Agent（网页操作版 + 命令行版）
  2. `bb3d19c` 优化：网页版提速（独立会话/RAG空库跳过/复核开关）、修复功能1记忆（指代词解析）、启动器改用绝对路径
  3. `bf67639` 添加 MIT License
  4. `3da4b65` 更新：修复Word乱码（MathType符号映射+JSON宽容解析）、修复双网页、功能3/4支持多文件选择处理、一键启动脚本加固（自动清理残留进程）
  5. `da0bbe0` 更新：功能3/4实时进度显示、防卡死保护（num_predict+timeout）、开发历程记录补充
  6. 本次提交：功能2 **升级为 Karpathy LLM Wiki**（编译词条 + 交叉链接 + 总索引 + Wiki优先检索 + 自检），开发历程记录补充
- **可用形态**：双击 `启动网页版.bat` 一键启动（自动处理残留进程、只开一个浏览器）；命令行版备用

---

## 六、经验沉淀

1. **本地大模型应用**：拆题/推理分模型分工，兼顾质量与速度
2. **网页版排障**：端口占用类的"无法启动"问题，要区分"端口有东西"和"页面真能用"，以 HTTP 200 为准
3. **中文乱码三类来源**：脚本编码（用 UTF-8/BOM）、Word 公式字体私有区（用符号映射）、AI 输出 JSON 不合法（用宽容解析）——都已各自根治
4. **GitHub 网络问题**：git 默认不走 Windows 系统代理，遇"连接被重置"先检查本地代理（Clash 等）并显式给 git 配置
5. **本地大模型长任务要有"反馈 + 兜底"**：长时间运行需加**实时进度反馈**与**单次推理上限/超时兜底**，否则 GPU 满载 ≠ 在推进，数小时无输出时无从判断
6. **判断单次推理是否卡死的硬指标**：用 GPU 算力对照单次正常耗时（RTX 4070 + 14B 模型单次应 1~2 分钟）；Ollama `/api/ps` 的 `expires_at` 长时间冻结 = 同一请求未结束，可判定异常
7. **LLM Wiki 优于裸 RAG 的关键**：一次编译、反复复用、带交叉引用；查询直接读结构化词条，比每次都重新嵌入检索原始文档更快更稳
8. **中文短查询检索要拆词**：无分隔符的中文查询用 2~3 字 n-gram 滑动窗口打分，能显著提升命中（如“导数求极值”→“极值/导数”）
